#import math
from math import sqrt, ceil
num = int (input('Digite um número: '))
print('a raiz quadrada é: {:.2f} '.format(sqrt(ceil(num))))
#print('a raiz quadrada é: {:.2f} '.format(math.sqrt(num))) essa forma quando importa a biblioteca geral se usa o nome da biblioteca.metodo
